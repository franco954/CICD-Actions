name: "Say Hello"

on:
  push:
    branches:
      - master

jobs:
  hello_world:
    runs-on: ubuntu-latest
    steps:
      - name: Echo the message
        run: |
          echo "Hello this is my first workflow"
      - name: Dime la fecha 
        run: date
      - run: ls -l

